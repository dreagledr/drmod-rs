Part of [#4](https://github.com/dreagledr/drmod-rs/issues/4).

**Проблема:** сплит на конце миссии R-07 не срабатывает.

**Референс:** автосплиттер LiveSplit ASL — https://github.com/hau5test/livesplit_asl_mgrr
Нужно сверить все окончания и старты сегментов с логикой из этого репо.

**Где:** `src/segment.rs` — функция `segment_action()`, строки 177–185:

```rust
if seg.mission_id == 0x0710
    && game_menu_status == GameMenuStatus::InGame
    && (pos.x - (-195.73)).abs() <= 0.1
    && (pos.y - (-7.1)).abs() <= 0.1
    && (pos.z - (-491.38)).abs() <= 0.1
{
    return SegmentAction::End;
}
```

Текущее условие: позиция `(-195.73, -7.1, -491.38)` с допуском ±0.1 и статус `InGame`.

**Что сделать:**
- Проверить корректность координат конца R-07 по автосплиттеру (возможно, позиция не совпадает с реальной в игре)
- Возможно, нужен другой статус (не `InGame`, а что-то другое — например `InMenu` как у остальных переходов)
- Сверить все окончания и старты сегментов (R-00→R-01, …, R-06→R-07) с эталонной логикой из [livesplit_asl_mgrr](https://github.com/hau5test/livesplit_asl_mgrr)
- Возможно, добавить отладочный вывод текущей позиции при входе в условие конца R-07
