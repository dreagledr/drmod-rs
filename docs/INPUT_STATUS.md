# Статус верификации входов API (2026-08-19)

Полный список команд `input` (docs/API.md §4.2) с результатами валидации сессии
«выбор → скрипт → логи → визуальный фидбек» (P118_BEACH, debug-сборка).

Легенда: ✅ работает · ❌ не работает · ⚠️ частично · ❓ не тестировано (нужны условия)

## Таблица входов

| Вход | Статус | Механизм | Комментарий |
|------|--------|----------|-------------|
| `forward` | ✅ | бит `0x400000` + `left_stick=[0,-1000]` | проверено |
| `backward` | ✅ | бит `0x800000` + `left_stick=[0,1000]` | проверено ранее |
| `left` | ✅ | бит `0x200000` + `left_stick=[-1000,0]` | проверено ранее |
| `right` | ✅ | бит `0x100000` + `left_stick=[1000,0]` | проверено ранее |
| `jump` | ✅ | бит **`0x10`** + фронт `pressed=0x10`; **высота зависит от удержания**: 1 кадр ≈ 1.7 м, 45 кадров ≈ 2.8 м (полная) | 🔧 фикс: настоящий бит 0x10 (было 0x1 — тот открывает меню оружия, §10.1). Двойного прыжка нет |
| `light_attack` | ✅ | бит `0x40`, down + pressed на 1-м кадре | работал ранее, в сессии использовался (обнажение меча) |
| `heavy_attack` | ✅ | бит `0x80`, down + pressed на 1-м кадре | проверено ранее, в сессии не перепроверялся |
| `camera` `[dx,dy]` | ✅ | `right_stick=[dx,dy]` | yaw и pitch работают; **чувствительность низкая** — значения нужны как у реальной мыши (сотни–тысячи): `[0,2000]` (60 кадров) наклонил камеру с +0.17 до −1.13 рад; `[0,200]` ниже порога игры (§10.4) |
| `ripper` | ✅ | фронт `isKeybindPressed(11)`, 1 игровой тик | 🔧 фикс: счёт в `script_tick` (сброс в начале кадра), а не декремент в детуре — игра вызывает isKeybindPressed(11) спорадически. Вкл/выкл чисто, на бегу держится. Второй путь — биты InputUnit `0x1000`/`0x8000` (L3/R3) |
| `blade` | ✅ | бит **`0x800`** + фронт `pressed=0x800` | 🔧 фикс: игра кодирует блейд битом 0x800 в InputUnit (как ninja 0x4000) — реальный ввод даёт `down=00400800` (запись 33 в БД). Keybind-эмуляция `isKeybindDown(8)` НЕ работает: игра читает её только в key-event обработке (0x61DA85), скрипт key events не создаёт. Механизм — как в playback (replay.rs). ✅ проверено live (2026-08-18): `blade=2`, `r_anim=69` все 60 кадров, снятие после скрипта |
| `ninja_run` | ✅ | бит **`0x4000`** + фронт `pressed=0x4000` | 🔧 фикс: для unit 0 игра читает бит, а не isKeybindDown(9). Скорость ~7.9 у/с (r_anim=71 при движении, 79 в покое). ⚠️ тестировать на открытом участке (стена искажает скорость) |
| `walk` | ✅ | `left_stick` ×0.5 | 🔧 фикс: ходьба = магнитуда стика (ручная: L=(0,-500), r_anim=2); keybind 4 игра не читает |
| `dodge` | ✅ | hold `isKeybindDown(21)` | r_anim 0→99, перекат. Перепроверен (3 прогона) |
| `lock_on` | ❌ | hold `isKeybindDown(12)` | ожидается фейл (цикл 5..22 не влияет на unit 0) — ❓ по отдельности не перепроверялся (нужен враг) |
| `subweapon` | ✅ | hold `isKeybindDown(13)` | проверено (2026-08-19): долгое удержание (120 кадров) → режим прицеливания; короткий тап (2 кадра) → мгновенное применение без прицеливания. Игра кодирует subweapon битом `0x400` в InputUnit (фронт `pressed=0x400` на 1-м кадре, дальше `down=0x400`) |
| `item` | ✅ | hold `isKeybindDown(14)` | проверено (2026-08-19): применение предмета (и долгое удержание, и короткий тап). Режима прицеливания у item нет — в отличие от subweapon (13) |
| `ar_mode` | ✅ | бит **`0x08`** + фронт `pressed=0x08` | 🔧 фикс: игра кодирует AR-режим битом 0x08 в InputUnit (как прыжок 0x10) — raw-подача через кэш `ms_KeyInput` не работает (§10.3). Механизм — как у прыжка: бит + фронт pressed на 1-м кадре. ✅ проверено live (2026-08-19) |
| `weapon_select` | ✅ | бит **`0x01`** (DPAD_LEFT) + фронт; **гейт по GameMenuStatus** | 🔧 фикс 2026-08-19: бит подаётся только пока `GameMenuStatus != SelectWeaponMenu` — игра сама открывает меню, бит не листает слоты (в меню бит 0x01 = навигация влево). Настоящий ввод, совместим с записью/воспроизведением (TAS). ✅ проверено live |
| `codec` | ❌ | raw-клавиша (3) | механизм raw-кэша не работает (§10.3) — нужен хук isKeyDown/isKeyPressed |
| `zandatsu` | ❓ | hold `isKeybindDown(20)` | keybind 20 имеет отдельный call site (может работать) — ❓ не тестировался (нужен враг) |
| `camera_reset` | ❌ | hold `isKeybindDown(19)` | ожидается фейл — ❓ не перепроверялся |
| `pause` | ✅ | BUTTON_B (бит `0x20`) | 🔧 фикс 2026-08-19: Esc = BUTTON_B (отмена/назад) в меню — конвертация в playback (raw 0x8E → бит 0x20). ✅ проверено live в playback |
| `confirm` | ✅ | D-Pad BUTTON_A (бит `0x10`), **2 кадра** | 🔧 фикс 2026-08-19: 1 кадр слишком короткий — игра не успевает обработать. 2 кадра подтверждают и закрывают меню. ✅ проверено live |
| `menu_up/down/left/right` | ✅ | D-Pad биты `0x08`/`0x04`/`0x01`/`0x02` | 🔧 фикс 2026-08-19: меню навигируется геймпадными D-Pad битами InputUnit (не клавишами/хуком isKeyDown). `menu_down` (0x04) проверен live — курсор двигается |
| `left_stick` `[x,y]` | ✅ | явный стик | 🔧 фикс: не выставлял `active` (override не применялся). Стик сам управляет движением — биты направлений не обязательны. Диагонали проверены |

## Сводка

- **Работает (после фиксов):** движение 4 стороны, `left_stick`, `jump` (бит 0x10 + удержание),
  `walk` (стик ×0.5), `ninja_run` (бит 0x4000 + фронт), `blade` (бит 0x800 + фронт),
  `ripper` (фронт 1 тик), `dodge`, `subweapon` (hold `isKeybindDown(13)`; долгое удержание →
  прицеливание, короткий тап → мгновенное применение; игра кодирует его битом `0x400` в InputUnit),
  `item` (hold `isKeybindDown(14)`; применение предмета — режима прицеливания нет, в отличие
  от subweapon), `ar_mode` (бит 0x08 + фронт), `weapon_select` (бит 0x01 +
  гейт по GameMenuStatus — настоящий ввод), `menu_up/down/left/right`
  (D-Pad биты 0x08/0x04/0x01/0x02), `confirm` (BUTTON_A 0x10, 2 кадра),
  `pause`/Esc (BUTTON_B 0x20, отмена в меню),
  `camera` (yaw + pitch; значения как у реальной
  мыши, см. таблицу), `light_attack`/`heavy_attack` (ранее).
- **Не работает:** цифра 3 (codec, raw-кэш), `lock_on`/`camera_reset` (цикл 5..22).
  ⚠️ Вывод «цикл 5..22 не влияет на unit 0» уточнён (2026-08-19):
  для `subweapon` (13) и `item` (14) keybind-удержание РАБОТАЕТ — игра ставит бит в InputUnit;
  не работают только меню-действия (16/18 и т.п.), которым нужен другой путь.
- **Не тестировано (нужен враг/условия):** `zandatsu`, `lock_on`.

## Тест-скрипты

`test_inputs/*.json` — по одному на вход (left_stick_diag, camera, walk, ninja_run, jump,
jump_hold, double_jump, dodge, ripper*, blade*, run_control, bunnyhop, movement_4dir,
subweapon_hold, subweapon_tap, item_hold, item_tap) +
сохранённые логи `test_inputs/logs_*.json`.

## Детали фиксов (2026-08-18)

- `left_stick` явный: `active = true` в `script_tick`.
- `walk`: масштабирование стика ×0.5 (игра кодирует ходьбу магнитудой, keybind 4 не читает).
- `ninja_run`: бит `0x4000` + фронт pressed (для unit 0 keybind-путь не работает).
- `blade`: бит `0x800` + фронт pressed (как ninja 0x4000) — игра кодирует блейд этим битом
  в InputUnit (запись 33 в БД: `down=00400800`); keybind-эмуляция `isKeybindDown(8)` не
  работает (игра читает её только в key-event обработке 0x61DA85). Механизм — как в
  playback (replay.rs): подача InputUnit с битом 0x800 через override.
- `jump`: бит `0x10` (эмпирически), высота от длительности удержания.
- `ripper`: фронт `isKeybindPressed(11)` живёт ровно 1 игровой тик — сброс в начале
  `ApiServer::frame_update` (для script_tick, playback и NumPad7), детур не декрементит.
- Камера в `/state` и `/logs`: `camera_pos`, `camera_look_at`, `camera_rot` [yaw, pitch, roll].
- `camera` pitch: работает через `right_stick[1]`, но чувствительность низкая — порог
  ~500–600 (значение 200 камеру не двигает). Реальная мышь даёт 500–6500; тест `[0,2000]`
  (60 кадров) наклонил камеру с +0.17 до −1.13 рад (кламп ~±65°). Механизм тот же, что
  в record/playback (override `g_InputUnit0`).

## Верификация subweapon (2026-08-19)

- **Долгое удержание** (`subweapon: true`, 120 кадров) → режим прицеливания ✅ (визуально).
- **Короткий тап** (`subweapon: true`, 2 кадра) → мгновенное применение без прицеливания ✅ (визуально).
- Механизм — hold `isKeybindDown(13)` (без изменений кода, реализация уже работала).
- Игра кодирует subweapon битом **`0x400`** в InputUnit (debug.log): 1-й кадр
  `down=00000400 pressed=00000400` (фронт), далее `down=00000400` (удержание).
  Бит ставит сама игра из keybind-удержания (наш override пустой) — цикл 5..22
  (0x61DE21) влияет на unit 0 для subweapon, в отличие от меню-действий (16/18).
- Тест-скрипты: `test_inputs/subweapon_hold.json`, `test_inputs/subweapon_tap.json`.

## Верификация item (2026-08-19)

- **Долгое удержание** (`item: true`, 120 кадров) → применение предмета ✅ (визуально).
- **Короткий тап** (`item: true`, 2 кадра) → применение предмета ✅ (визуально).
- Механизм — hold `isKeybindDown(14)` (без изменений кода, реализация уже работала).
- У `item` **нет режима прицеливания** (в отличие от subweapon 13) — и долгое удержание,
  и короткий тап просто применяют предмет.
- Тест-скрипты: `test_inputs/item_hold.json`, `test_inputs/item_tap.json`.
